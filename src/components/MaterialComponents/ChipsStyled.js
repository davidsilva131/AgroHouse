import { styled } from '@mui/material/styles';
import { Chip } from "@mui/material";

export const EditChip = styled(Chip)({
  backgroundColor: '#006837',
  color: 'white'
});

export const DeleteChip = styled(Chip)({
  backgroundColor: 'red',
  color: 'white'
});